# macOS
.DS_Store
.AppleDouble
.LSOverride
._*
.Spotlight-V100
.Trashes
.fseventsd

# Secrets & private config
.env
*.secret
secrets/

# Editor directories
.vscode/
.idea/
*.swp
*.swo

# Logs
*.log

# Local-only files (not pushed to remote)
CHANGELOG.md

# Per-machine private briefing for reinstall / agents (API models, specs, IPs, …)
# Commit machine.md.example only — never machine.md / machine-*.md
machine.md
**/machine.md
machine-*.md
!machine.md.example

# Private Pass maps (inventory / titles) — examples are committed
scripts/pass-ssh-key-map.local
scripts/pass-env-map.local
**/pass-ssh-key-map.local
**/pass-env-map.local

# Optional private AI notes (public AGENTS.md is committed; CLAUDE.md is obsolete)
AGENTS.local.md
CLAUDE.md
CLAUDE.local.md
GEMINI.md
OPENCODE.md
CODEX.md
COPILOT.md

# Secret scan temp
.gitleaks-report.json
